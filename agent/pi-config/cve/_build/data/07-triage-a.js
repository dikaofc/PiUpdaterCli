// CVE skill data — triage A (8)
export const SKILLS = [
{ file: 'cve-triage-engine.md', title: 'CVE Triage Engine', cat: 'triage', keywords: 'triage, prioritization, scoring',
  purpose: 'Score and rank CVE candidates into a triage queue: exploitability signals, reachability classification, severity, asset criticality, and remediation cost.',
  triggers: 'Any time a candidate list exists (from discovery+applicability+reachability) and must be ordered for the review.',
  method: [
    'Ingest each candidate with its applicability verdicts, reachability classification, and evidence level.',
    'Compute the triage inputs: severity (source-validated CVSS), reachability class, KEV/exploitation status, asset criticality (from asset inventory), exposure (network-facing).',
    'Combine into a transparent priority score; keep the score decomposable so any input can be explained.',
    'Apply the rules: KEV raises priority; DIRECTLY_REACHABLE + KEV is highest; UNKNOWN reachability is ranked by severity only with a visible caveat.',
    'Never present the score as certainty - the triage queue is a review order, not a verdict.',
  ],
  evidence: 'A triage queue with per-item scores, the inputs behind each score, and the ordering rationale.',
  rootCause: 'Sorting by severity alone (or by lockfile presence alone).',
  impact: 'Wasted review on unreachable high-CVSS items; missed exploited ones.',
  remediation: 'Multi-input scoring with reachability weight, KEV boost, asset criticality, and decomposable scores.',
  regression: 'Triage fixture sets asserting expected ordering.',
  fp: 'Scores that outrank a DIRECTLY_REACHABLE item due to a single inflated input - validate weight balance.',
  related: ['cve-severity-analysis', 'cve-known-exploitation-priority', 'cve-priority-model'],
  refs: ['risk scoring literature', 'CVSS risk scoring guidance'],
},
{ file: 'cve-severity-analysis.md', title: 'CVE Severity Analysis', cat: 'triage', keywords: 'cvss, severity, base score',
  purpose: 'Select, validate, and contextualize the severity for a CVE: which CVSS version, which source, and what the local context changes.',
  method: [
    'Collect all (version, source, vector, score) pairs from the normalized record (cvss-normalization).',
    'Select the canonical value by source priority, preferring the most authoritative and current version.',
    'Validate the vector: parse it, recompute the score, flag mismatches.',
    'Adjust for local context only where justified: the CVE severity is a property of the vulnerability, not of the project - project risk is computed in triage.',
    'Record severity with version + source + computed date.',
  ],
  evidence: 'Canonical severity with version/source/vector validation results and recomputed score match.',
  rootCause: 'Blindly trusting the highest number or an unvalidated vector.',
  impact: 'Triage skew from wrong severity.',
  remediation: 'Validated canonical selection, recompute checks, context separation.',
  regression: 'Vector validation and selection fixture tests.',
  fp: 'Treating the CVE severity as the project risk - they differ; use triage for project risk.',
  related: ['cvss-normalization', 'cvss-v3-analysis', 'cve-triage-engine'],
  refs: ['CVSS spec v3.1/v4.0', 'FIRST calculators'],
},
{ file: 'cvss-v3-analysis.md', title: 'CVSS v3 Analysis', cat: 'triage', keywords: 'cvss v3, vector parsing, environmental',
  purpose: 'Analyze CVSS v3.0/3.1 vectors and scores in detail: parse components, understand metric semantics, and compute/recompute scores.',
  method: [
    'Parse v3.x vectors: AV, AC, PR, UI, S, C, I, A.',
    'Apply the v3.1 base equation to recompute the score from the vector; flag discrepancies with published values.',
    'Interpret metric semantics for impact statements (e.g., Scope=Changed cases).',
    'Assess temporal/environmental metrics only when explicitly provided; do not invent them.',
    'Output the canonical base score with vector breakdown.',
  ],
  evidence: 'Vector parse + recomputed score + semantic notes; discrepancies flagged.',
  rootCause: 'Score copied from aggregators without vector validation.',
  impact: 'Wrong severity in triage and reports.',
  remediation: 'Recompute-from-vector checks, semantic interpretation, provenance.',
  regression: 'CVSS v3 recompute test vectors (official examples).',
  fp: 'v3.0 vs v3.1 differences treated as errors - both are valid with version tagging.',
  related: ['cvss-normalization', 'cve-severity-analysis'],
  refs: ['CVSS v3.1 specification', 'FIRST official examples'],
},
{ file: 'cvss-v4-analysis.md', title: 'CVSS v4 Analysis', cat: 'triage', keywords: 'cvss v4, vector, threat metrics',
  purpose: 'Analyze CVSS v4.0 vectors: new metrics (AT, VC/VI/VA, SC/SI/SA, MSI/MSA), and v4 semantics including derived metric groups.',
  method: [
    'Parse v4 vectors: AV, AC, AT, PR, UI, VC, VI, VA, SC, SI, SA (plus supplemental S/A, MSI/MSA, E/AU/R/V/RE/U).',
    'Understand v4 differences: no Scope, separate confidentiality/integrity/availability for vulnerable system vs subsequent system.',
    'Recompute the v4 score from the vector using the official equations (including derived base metrics).',
    'Handle v4 vulnerability vs subsequent-system combinations in impact interpretation.',
    'Tag records with v4 version explicitly; note that many records still carry only v3.',
  ],
  evidence: 'v4 vector parse with recomputed score and semantic notes.',
  rootCause: 'v4 vectors treated with v3 semantics (or ignored).',
  impact: 'Mis-scored v4 records as v4 adoption grows.',
  remediation: 'Version-explicit analysis, v4-specific recomputation, semantics documentation.',
  regression: 'Official CVSS v4 example vectors asserting recomputed scores.',
  fp: 'Mixing v4 and v3 scores in comparisons without version normalization.',
  related: ['cvss-normalization', 'cve-severity-analysis'],
  refs: ['CVSS v4.0 specification', 'FIRST v4 examples'],
},
{ file: 'cve-priority-model.md', title: 'CVE Priority Model', cat: 'triage', keywords: 'priority, urgency, business impact',
  purpose: 'Define and apply the project-level priority model: how severity, reachability, exploitation status, and asset criticality combine into an actionable priority.',
  method: [
    'Define the priority bands (e.g., Critical/High/Medium/Low/Informational) with explicit inclusion criteria per band.',
    'Set the critical band: DIRECTLY_REACHABLE or CONDITIONALLY_REACHABLE-with-common-condition + KEV or high severity + critical asset, evidence-gated.',
    'Set the medium band: PRESENT_BUT_UNUSED with future-risk potential, or reachable in non-default configs.',
    'Keep UNKNOWN-classified items visible in their own band with a review requirement, not merged silently.',
    'Publish the model so every priority label is reproducible from its inputs.',
  ],
  evidence: 'The priority model definition and a sample of records with the inputs behind each assigned band.',
  rootCause: 'Ad-hoc "high/medium/low" labels with no derivation rule.',
  impact: 'Inconsistent prioritization and unresolvable disputes.',
  remediation: 'Band criteria from explicit inputs, reproducibility, UNKNOWN visibility.',
  regression: 'Fixture records asserting band assignment.',
  fp: 'Applying band criteria without the required evidence level (evidence-gate first).',
  related: ['cve-triage-engine', 'cve-known-exploitation-priority', 'reachability-classification'],
  refs: ['risk matrix guidance'],
},
{ file: 'cve-known-exploitation-priority.md', title: 'CVE Known Exploitation Priority', cat: 'triage', keywords: 'kev, exploited, in the wild',
  purpose: 'Incorporate known-exploitation status (CISA KEV, vendor statements, threat intel) into priority while keeping it separate from reachability.',
  method: [
    'Check the CVE against the CISA KEV index and vendor exploitation statements (record source + date).',
    'Use exploitation status as a priority boost, NOT as evidence of project exploitability.',
    'For KEV-listed CVEs that are PRESENT_BUT_UNUSED in the project: still review for latent risk, but the report must state the code path is not currently used.',
    'Handle KEV additions retroactively: a KEV addition re-opens the CVE review (re-triage with the new signal).',
    'Record exploitationStatus: KNOWN_IN_THE_WILD / NOT_KNOWN / UNKNOWN with provenance.',
  ],
  evidence: 'KEV lookup result with date/source, the priority adjustment, and the reachability classification kept separate.',
  rootCause: 'KEV presence automatically declared as project exploitation.',
  impact: 'False "actively exploited in our app" claims.',
  remediation: 'KEV as priority input, reachability as applicability input, explicit separation in reports.',
  regression: 'Fixture cases: KEV + PRESENT_BUT_UNUSED produces the correct dual verdict.',
  fp: 'KEV entries referencing products/versions different from the project deployment.',
  related: ['cisa-kev-ingestion', 'cve-triage-engine', 'cve-priority-model'],
  refs: ['CISA KEV catalog', 'BOD 22-01'],
},
{ file: 'cve-advisory-severity-validation.md', title: 'CVE Advisory Severity Validation', cat: 'triage', keywords: 'advisory severity, ghsa, vendor severity',
  purpose: 'Validate severity/priority claims from advisories (GHSA, vendor CVSS) against the canonical normalized data and the vector evidence.',
  method: [
    'Compare advisory severity values (GHSA levels, vendor CVSS, OSV severity) with the canonical CVE severity.',
    'For each discrepancy, inspect the vector: which metrics drive the difference (e.g., vendor assumed network attack, NVD assumed adjacent).',
    'Record both values with the metric-level explanation of the difference.',
    'Prefer the vendor advisory severity for the vendor product, but keep NVD as comparison.',
    'Flag unsupported severity claims (no vector/score evidence) as unvalidated.',
  ],
  evidence: 'A severity-comparison table per CVE with metric-level difference explanations.',
  rootCause: 'Severity copied from whichever source was read first.',
  impact: 'Priority distortion from unvalidated advisory severity.',
  remediation: 'Cross-source severity comparison, metric-level diffs, provenance.',
  regression: 'Comparison fixtures with known metric differences.',
  fp: 'Version-validity differences (advisory for old version vs new) mistaken for metric disagreement.',
  related: ['cve-severity-analysis', 'advisory-correlation', 'cve-triage-engine'],
  refs: ['GHSA severity docs', 'CVSS metric definitions'],
},
{ file: 'cve-triage-review-workflow.md', title: 'CVE Triage Review Workflow', cat: 'triage', keywords: 'workflow, review, decision record',
  purpose: 'Run the human-in-the-loop triage review: queue, assign, analyze, decide, record — with auditable decision records.',
  method: [
    'Define the workflow stages: QUEUED -> ANALYZING (reachability/evidence work) -> DECIDED (affected/not/unknown + priority) -> REMEDIATING/CLOSED (justified).',
    'Assign each CVE to the appropriate skill path (applicability, reachability, validation, remediation).',
    'Require a decision record per CVE: inputs, evidence level, decision, rationale, and who/what produced it.',
    'Handle re-opens: new data (KEV, new advisory, version change) re-enters the queue.',
    'Produce per-sprint summary stats: triaged, decided, remediated, deferred (with reasons).',
  ],
  evidence: 'Workflow state per CVE with timestamps, decision records, and a summary report.',
  rootCause: 'Triage decisions made in chat/email with no record.',
  impact: 'Untraceable decisions; re-review waste; lost rationale.',
  remediation: 'State machine with decision records, re-open rules, summary reporting.',
  regression: 'Workflow transition tests (allowed/denied transitions).',
  fp: 'A workflow state shown as final without the required decision record - gate closure on the record.',
  related: ['cve-triage-engine', 'cve-data-consistency-check'],
  refs: ['decision-record patterns'],
},
];