// CVE skill data — remediation (6)
export const SKILLS = [
{ file: 'cve-fix-version-resolution.md', title: 'CVE Fix Version Resolution', cat: 'remediation', keywords: 'fix version, first fixed, upgrade target',
  purpose: 'Resolve the precise fix/upgrade target for each affected component: first fixed version per source, per ecosystem, and per deployment.',
  triggers: 'For every CVE with an affected verdict, the remediation path starts with the exact fix version.',
  method: [
    'Collect all fixed-version signals: OSV/GHSA fixed events, vendor advisory fixed lists, distro fixed versions, patch references.',
    'Normalize each per ecosystem and compute the first-fixed per source (fixed-version-extraction).',
    'Resolve the upgrade target: the minimum version that fixes the CVE while satisfying the project constraints (see upgrade-planning).',
    'Handle multi-source conflicts: preserve each source value; pick the conservative target (highest first-fixed) with rationale.',
    'If no fix exists, record NO_FIX_KNOWN with the source statement (UNKNOWN for any unresolved).',
  ],
  evidence: 'Fix-target record: per-source fixed versions, normalized first-fixed, chosen target, and the conflict rationale.',
  rootCause: 'Upgrade suggestions based on the first version seen in a single feed.',
  impact: 'Upgrades that do not actually fix the CVE (or fix to an incompatible version).',
  remediation: 'Multi-source fix collection, normalized comparison, conservative target selection.',
  regression: 'Fix-resolution fixtures asserting the correct target with conflicts.',
  fp: 'A "fixed" version that is lower than an affected version (data error) - flag, never trust.',
  related: ['fixed-version-extraction', 'cve-fixed-version', 'cve-upgrade-planning'],
  refs: ['OSV fixed events', 'vendor advisory version lists'],
},
{ file: 'cve-remediation-prioritization.md', title: 'CVE Remediation Prioritization', cat: 'remediation', keywords: 'remediation order, effort, risk',
  purpose: 'Order remediation work by combining risk (triage priority) with remediation cost (effort, compatibility, deployment risk).',
  method: [
    'Take the triage priority (risk side) and estimate remediation cost per item: upgrade effort, breaking-change risk, backport availability, test coverage.',
    'Build the remediation backlog: highest risk + lowest cost first; high risk + high cost items get explicit plan/schedule.',
    'Group by component: one upgrade may fix many CVEs (batch remediation).',
    'Account for dependencies of remediation (upgrade order within a stack).',
    'Re-verify after each remediation (see remediation-verification).',
  ],
  evidence: 'Remediation backlog with risk/cost inputs and ordering; batching rationale.',
  rootCause: 'Remediation ordered by severity alone or by whichever CVE was complained about.',
  impact: 'Wasted effort on low-risk items while exploited ones wait.',
  remediation: 'Risk x cost ordering, component batching, explicit plans for hard items.',
  regression: 'Backlog-ordering fixtures asserting expected sequence.',
  fp: 'Double-counting risk when multiple CVEs share one fix - batch by component.',
  related: ['cve-triage-engine', 'cve-upgrade-planning', 'cve-priority-model'],
  refs: ['risk-based remediation literature'],
},
{ file: 'cve-patch-analysis.md', title: 'CVE Patch Analysis', cat: 'remediation', keywords: 'patch analysis, diff review, root cause',
  purpose: 'Analyze the fixing patch to understand the root cause, confirm the fix actually addresses the vulnerability, and inform backporting.',
  method: [
    'Fetch the cached patch (patch-extraction) and review the diff: which files/functions changed and how.',
    'Identify the root-cause pattern (input validation, missing check, unsafe parsing, logic error).',
    'Verify the fix closes the identified vulnerable behavior (does the diff cover all entry paths to the flaw?).',
    'Note incomplete fixes (patch covers one path, other paths remain) as findings.',
    'Use the analysis for backport assessment and for reachability validation (does the project exercise the fixed code?).',
  ],
  evidence: 'Patch review notes: files changed, root-cause pattern, fix-completeness assessment.',
  rootCause: 'Treating the patch as a black box and trusting the fix claim.',
  impact: 'False confidence in partial fixes; missed bypasses.',
  remediation: 'Diff-level review, root-cause mapping, completeness checks.',
  regression: 'Fixture patches (complete vs partial) asserting completeness verdicts.',
  fp: 'Patches that refactor unrelated code alongside the fix - focus on the fix-relevant hunks.',
  related: ['patch-extraction', 'cve-backport-assessment', 'cve-root-cause-analysis'],
  refs: ['CVE reference patches'],
},
{ file: 'cve-backport-assessment.md', title: 'CVE Backport Assessment', cat: 'remediation', keywords: 'backport, old version, security patch',
  purpose: 'Assess whether a fix can/should be backported to an older version that cannot upgrade, and whether a backport exists.',
  method: [
    'Check for existing backports: vendor-maintained old-version security releases, distro backports, community backports.',
    'If no backport exists, assess feasibility: does the patch apply cleanly to the target version, and are the surrounding APIs compatible?',
    'Assess safety: backported fix must preserve the target version behavior; conflict resolution must not drop fix hunks.',
    'Verify a candidate backport: apply in a controlled environment, run the target version tests, and confirm the vulnerable path is closed.',
    'Record the backport with verification evidence; if infeasible, document alternatives (mitigation, isolation).',
  ],
  evidence: 'Backport assessment: existing backport found/verified, or feasibility analysis with apply/test results.',
  rootCause: 'Upgrade-only remediation leaves old deployments unprotected.',
  impact: 'Long-running vulnerable versions without a path to safety.',
  remediation: 'Backport-first for pinned old versions, verification before adoption, mitigation fallback.',
  regression: 'Backport fixtures (applies cleanly vs conflict) asserting assessment outcomes.',
  fp: 'A patch that "applies" but is functionally wrong for the older code (behavioral regression) - verify, not just apply.',
  related: ['cve-patch-analysis', 'cve-fix-version-resolution', 'cve-upgrade-planning'],
  refs: ['distro backport practices'],
},
{ file: 'cve-upgrade-planning.md', title: 'CVE Upgrade Planning', cat: 'remediation', keywords: 'upgrade plan, breaking changes, migration',
  purpose: 'Plan the actual upgrade: target version selection, breaking-change assessment, migration steps, and rollback.',
  method: [
    'Select the target version from fix-version-resolution, considering the project constraints (runtime, platform, feature needs).',
    'Assess breaking changes between current and target versions (release notes, migration guides, deprecations).',
    'Identify affected project code (APIs used that changed) and required migration edits.',
    'Plan verification: tests, staging deploy, canary; define rollback criteria.',
    'Document the plan and record expected CVE closure per upgrade.',
  ],
  evidence: 'Upgrade plan: target version, breaking-change list, migration steps, verification/rollback plan.',
  rootCause: 'Version bumped blindly to the latest.',
  impact: 'Upgrade-induced incidents and unplanned rework.',
  remediation: 'Constraint-aware target selection, breaking-change inventory, verification planning.',
  regression: 'Upgrade-plan fixtures (dependency constraints, breaking-change lists) asserting target and steps.',
  fp: 'Latest version assumed compatible - verify against the project dependency constraints.',
  related: ['cve-fix-version-resolution', 'cve-remediation-prioritization', 'cve-backport-assessment'],
  refs: ['release notes and migration guides'],
},
{ file: 'cve-remediation-verification.md', title: 'CVE Remediation Verification', cat: 'remediation', keywords: 'verify fix, re-scan, regression',
  purpose: 'Verify that remediation actually closed the CVE: re-run discovery/applicability/reachability for the new version and confirm the verdict flips.',
  method: [
    'After remediation, re-resolve the lockfile/inventory to the new versions (in a controlled environment).',
    'Re-run the affected-version check: the CVE should now be NOT_AFFECTED (>= fixed) or the vulnerable functionality removed.',
    'Confirm no new CVEs were introduced at unacceptable risk by the upgrade (delta scan).',
    'Re-run the regression suite for the upgraded component.',
    'Record the verification result and close the CVE record with the evidence.',
  ],
  evidence: 'Post-remediation verification: new version in inventory, verdict flip evidence, delta scan, regression results.',
  rootCause: 'Remediation declared done when the lockfile was updated but the deploy was not verified.',
  impact: '"Fixed" CVEs still live in production.',
  remediation: 'Version-verified closure, deploy verification, delta scans, regression evidence.',
  regression: 'Closure-gate tests requiring the verdict flip before closing.',
  fp: 'Closing on the manifest change while the deployed artifact still carries the old version.',
  related: ['cve-fix-version-resolution', 'cve-upgrade-planning', 'cve-triage-review-workflow'],
  refs: ['remediation verification practices'],
},
];