// CVE skill data — triage B: KEV + exploitation-status (3)
export const SKILLS = [
{ file: 'cve-kev-correlation.md', title: 'CVE KEV Correlation', cat: 'triage', keywords: 'kev correlation, cisa, exploit status',
  purpose: 'Correlate every ingested CVE record against the CISA KEV catalog: match by CVE ID, track dates, and record known-ransomware signals.',
  method: [
    'Maintain a KEV-derived index keyed by canonical CVE ID (from cisa-kev-ingestion).',
    'On ingestion of each CVE record, look up KEV membership; store knownExploitation, dateAdded, dueDate, requiredAction, ransomwareUse.',
    'Detect KEV changes over time: new entries, removals, due-date changes; trigger re-triage for changed records.',
    'Surface KEV-flagged records prominently in the triage queue with the required action context.',
    'Never merge KEV status into the reachability classification (they answer different questions).',
  ],
  evidence: 'Correlation run report: KEV-matching CVEs with their KEV fields and change detection results.',
  rootCause: 'KEV treated as a static lookup with no date/change awareness.',
  impact: 'Stale KEV flags and missed re-triage on KEV updates.',
  remediation: 'Indexed correlation, change detection, re-triage triggers.',
  regression: 'Fixture KEV changes asserting correct flag updates.',
  fp: 'KEV entries for product families broader than the project component (verify product match).',
  related: ['cisa-kev-ingestion', 'cve-known-exploitation-priority', 'cve-triage-engine'],
  refs: ['CISA KEV catalog'],
},
{ file: 'cve-exploitation-status-analysis.md', title: 'CVE Exploitation Status Analysis', cat: 'triage', keywords: 'exploit status, exploitdb, public exploit',
  purpose: 'Determine the exploitation status of a CVE from authoritative sources (KEV, vendor statements, public exploit availability) with provenance.',
  method: [
    'Primary signals first: CISA KEV (known in-the-wild exploitation), vendor exploitation statements, CISA advisories.',
    'Secondary: exploit-database availability (Exploit-DB, Metasploit modules, GitHub PoC repos) - evidence of weaponization interest, not necessarily in-the-wild use.',
    'Record per-signal: status (confirmed exploited / PoC exists / no evidence), source, date, and confidence.',
    'Distinguish statuses in reports: "confirmed in-the-wild" vs "public PoC exists" vs "no public evidence".',
    'Combine into exploitationStatus with a conservative default: NOT_KNOWN when no evidence, not "not exploited".',
  ],
  evidence: 'Exploitation-status record with per-signal provenance and a confidence level.',
  rootCause: 'Absence of public PoC treated as "not exploitable".',
  impact: 'False confidence in remediation priority.',
  remediation: 'Multi-signal status with provenance, conservative defaults, PoC != in-the-wild labeling.',
  regression: 'Status fixtures asserting the conservative default and signal priority.',
  fp: 'PoC repos that are broken/non-functional - availability without verification is a weaker signal.',
  related: ['cve-kev-correlation', 'cve-known-exploitation-priority', 'cve-triage-engine'],
  refs: ['Exploit-DB', 'Metasploit', 'CISA advisories'],
},
{ file: 'cve-kev-false-positive-filter.md', title: 'CVE KEV False Positive Filter', cat: 'triage', keywords: 'kev fp, product mismatch, version mismatch',
  purpose: 'Filter false positives that arise from KEV automation: product mismatches, version mismatches, and misattributed exploitation.',
  method: [
    'For each KEV-flagged CVE, verify the KEV product/vendor description matches the project component (product mismatch is a FP).',
    'Verify version coverage: KEV entries often lack versions; check whether the project version is within any stated affected range.',
    'Check platform: KEV items may target OS/browser/hardware not present in the deployment.',
    'Confirm the component is actually present (inventory) and the vulnerable functionality exists (applicability).',
    'Output the FP verdict with the mismatch evidence; keep the KEV flag in the record for audit.',
  ],
  evidence: 'Per-KEV-CVE verification: product, version, platform, presence, functionality - with the FP verdict and evidence.',
  rootCause: 'KEV automation flagging every CVE in the catalog without local verification.',
  impact: 'Priority queue polluted by inapplicable KEV entries.',
  remediation: 'Local verification before priority elevation, mismatch evidence, audit-preserved KEV flag.',
  regression: 'FP fixtures (product mismatch, version mismatch, absent component) asserting verdicts.',
  fp: 'This skill is about filtering; be careful not to filter on insufficient evidence (absence of version data is not a mismatch).',
  related: ['cve-kev-correlation', 'cve-known-exploitation-priority', 'cve-version-matching'],
  refs: ['CISA KEV catalog semantics'],
},
];