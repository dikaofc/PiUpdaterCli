// CVE skill data — extras (4)
export const SKILLS = [
{ file: 'cve-confidence-model.md', title: 'CVE Confidence Model', cat: 'triage', keywords: 'confidence, evidence level, uncertainty',
  purpose: 'Model and record confidence for every CVE verdict (applicability, reachability, severity, exploitation) with explicit uncertainty.',
  triggers: 'Attach confidence to every verdict produced by the engine; final reports mirror these levels.',
  method: [
    'Define confidence levels per verdict type: HIGH (verified direct evidence), MEDIUM (strong indirect evidence), LOW (inference), UNKNOWN (insufficient data).',
    'Bind confidence to evidence: each level requires a minimum evidence tier (E-level) and a source set.',
    'Record confidence on the CVE record with the evidence references.',
    'Auto-downgrade: missing required evidence downgrades the verdict automatically.',
    'Surface UNKNOWN prominently: an UNKNOWN is a finding state, not a clean state.',
  ],
  evidence: 'Confidence-tagged record sample with evidence references and downgrade behavior demonstrated.',
  rootCause: 'Confidence implicit and unrecorded.',
  impact: 'Reports that read as fact where only inference exists.',
  remediation: 'Explicit confidence model, evidence-bound levels, auto-downgrade, UNKNOWN surface area.',
  regression: 'Confidence fixtures asserting level assignment and downgrades.',
  fp: 'HIGH confidence claimed on tool output without tool-validated evidence.',
  related: ['reachability-evidence-model', 'cve-data-validation', 'cve-triage-engine'],
  refs: ['evidence model in root docs'],
},
{ file: 'cve-knowledge-gap-analysis.md', title: 'CVE Knowledge Gap Analysis', cat: 'validation', keywords: 'gap analysis, missing data, unknown coverage',
  purpose: 'Track and report knowledge gaps in the CVE dataset: missing descriptions, missing ranges, unreviewed records, unenriched data.',
  method: [
    'Define gap dimensions per record: no description, no CWE, no affected range, no severity vector, undefined exploitation status, no patch data.',
    'Compute per-dimension coverage stats across the database.',
    'Prioritize gaps by impact: unexploitable-blind (no ranges) and unverifiable (no patch) gaps rank highest.',
    'Route gap-filling: vendor advisory lookup, reviewer enrichment (tagged), downstream sources.',
    'Report gap summary in every engine summary so coverage claims stay honest.',
  ],
  evidence: 'Gap report with per-dimension coverage and the prioritized fill-backlog.',
  rootCause: 'Records treated as complete because they exist.',
  impact: 'Silent blind spots misread as clean data.',
  remediation: 'Gap tracking, prioritized enrichment, honest coverage reporting.',
  regression: 'Gap fixtures asserting dimension detection.',
  fp: 'Gaps that are inherent to the vulnerability (no data exists anywhere) - still UNKNOWN, but with a "no data anywhere" note.',
  related: ['cve-data-validation', 'cve-confidence-model', 'cve-data-consistency-check'],
  refs: ['data-quality assessment practices'],
},
{ file: 'cve-cache-rebuild.md', title: 'CVE Cache Rebuild', cat: 'ingestion', keywords: 'cache rebuild, rebuild, migration',
  purpose: 'Rebuild the local CVE caches/databases safely: from raw data, from normalized data, or from scratch — with verification.',
  method: [
    'Decide rebuild scope: rebuild from raw (full re-normalization), from normalized (indexes only), or partial (one source).',
    'Back up the current state (database/cache) before rebuilding; the rebuild must be reversible.',
    'Run the pipeline in the chosen scope with schema validation and idempotent writes.',
    'Verify post-rebuild: record counts match, integrity checks pass, no duplicate canonical IDs.',
    'Verify offline usability: all query paths work without network after the rebuild.',
  ],
  evidence: 'Rebuild log with scope, verification counts, and post-rebuild integrity results.',
  rootCause: 'Cache repairs done in-place without backup or verification.',
  impact: 'Corrupted or lost data invisible until query time.',
  remediation: 'Scoped rebuilds, backups, verification gates, offline checks.',
  regression: 'Rebuild fixtures asserting count/integrity verification.',
  fp: 'Rebuilding when an incremental update would suffice - prefer incremental for routine freshness.',
  related: ['cve-feed-ingestion', 'cve-data-validation', 'cve-normalization'],
  refs: ['backup/restore practices'],
},
{ file: 'cve-audit-report-generation.md', title: 'CVE Audit Report Generation', cat: 'remediation', keywords: 'report, findings, summary',
  purpose: 'Generate the CVE audit report from the record set: findings, classifications, evidence, priorities, and the honest coverage statement.',
  method: [
    'Aggregate the triaged records with classifications, evidence levels, and priorities.',
    'Group findings: DIRECTLY_REACHABLE first, then CONDITIONALLY_REACHABLE (with conditions), PRESENT_BUT_UNUSED (latent), UNKNOWN (reviewable).',
    'For each finding: CVE, component, version, classification, evidence link, recommended action, source references.',
    'Include the coverage statement: sources ingested, last update, UNKNOWN/gap counts - no coverage overclaim.',
    'Produce the report in the template format (templates/cve-report.md) for hand-off.',
  ],
  evidence: 'Generated report matching the template with per-finding records and the coverage statement.',
  rootCause: 'Reports assembled ad hoc from notes.',
  impact: 'Untraceable, unrepeatable audit outputs.',
  remediation: 'Database-driven report generation, consistent structure, coverage honesty.',
  regression: 'Report fixtures asserting structure and required fields.',
  fp: 'Including UNKNOWN records only when explicitly reviewable - keep them in a clearly separated section.',
  related: ['cve-triage-engine', 'cve-knowledge-gap-analysis', 'cve-remediation-verification'],
  refs: ['templates/cve-report.md (this repo)'],
},
];