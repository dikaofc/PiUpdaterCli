// CVE skill data — validation (7)
export const SKILLS = [
{ file: 'cve-data-validation.md', title: 'CVE Data Validation', cat: 'validation', keywords: 'validation, schema, integrity',
  purpose: 'Validate CVE/advisory records against schemas and integrity rules at every stage of the pipeline.',
  method: [
    'Validate against the JSON schemas: cve, cpe, cvss, advisory, package (schemas/*.json).',
    'Run format checks: CVE ID syntax (CVE-YYYY-NNNNNN), date formats, vector component values, CWE id format, URL sanity.',
    'Run cross-field consistency checks: affected ranges vs fixed versions (fixed must not be inside affected), CVSS recompute matches score.',
    'Run referential checks: related IDs resolve, alias chains acyclic, package keys resolve in the ecosystem map.',
    'Report violations with severity; decide reject vs flag-and-continue per rule class.',
  ],
  evidence: 'Validation report: records validated, violations by class, and disposition decisions.',
  rootCause: 'No validation gates between pipeline stages.',
  impact: 'Corrupt records poison triage and correlation.',
  remediation: 'Schema + integrity validation at each stage, violation classes with dispositions.',
  regression: 'Invalid-record fixtures asserting rejection/flags.',
  fp: 'Format variations that are legal in one source but not another - validate against per-source rules first.',
  related: ['cve-normalization', 'cvss-normalization', 'cve-data-consistency-check'],
  refs: ['schemas/*.json in this repo'],
},
{ file: 'cve-false-positive-engine.md', title: 'CVE False Positive Engine', cat: 'validation', keywords: 'false positive, filter, triage reduction',
  purpose: 'Systematically apply the FP filter stack to remove or downgrade false positives before they enter the report.',
  method: [
    'Run the FP filters in order: duplicate/alias collapse, product/component mismatch, version out-of-range, platform/architecture mismatch, feature/config disabled, not-deployed (dev/optional), functionality not present, code path not imported/called, mitigation verified.',
    'Each filter requires evidence; filters without evidence do not apply (stay UNKNOWN).',
    'Record every FP verdict with the filter used and the evidence; keep the record in the database for audit.',
    'Periodically review filter accuracy (missed FPs vs wrongly filtered real findings) and tune.',
    'Never auto-delete: FP verdicts are reviewable and reversible.',
  ],
  evidence: 'FP-run report: candidates reduced by filter with per-item verdicts and evidence; a review trail.',
  rootCause: 'FP filtering by instinct or by a single heuristic.',
  impact: 'Noise floods the report or real findings get wrongly dropped.',
  remediation: 'Evidence-gated filter stack, recordable verdicts, accuracy review loop.',
  regression: 'FP filter fixtures asserting correct application order and verdicts.',
  fp: 'The FP engine itself produces false negatives when evidence is missing - missing evidence yields UNKNOWN, not "clean".',
  related: ['cve-kev-false-positive-filter', 'reachability-classification', 'cve-vulnerable-code-path'],
  refs: ['FP taxonomy in root docs'],
},
{ file: 'cvss-v3-vector-validation.md', title: 'CVSS v3 Vector Validation', cat: 'validation', keywords: 'v3 vector, validation, recompute',
  purpose: 'Validate CVSS v3.x vectors: component legality, value enumeration, and score recomputation.',
  method: [
    'Parse the vector into components (AV/AC/PR/UI/S/C/I/A) and validate each value against the v3 spec enumeration.',
    'Check the vector format (slash-separated, no spaces, allowed values).',
    'Recompute the base score with the official v3 equations; compare to the published score.',
    'Flag and record discrepancies; investigate the cause (rounding, environmental metrics, wrong version).',
    'Output validated canonical vector + score.',
  ],
  evidence: 'Vector validation results: parse OK/error, recompute match/mismatch, discrepancy analysis.',
  rootCause: 'Malformed or mis-versioned vectors in source data.',
  impact: 'Wrong severity numbers propagate.',
  remediation: 'Strict vector validation, recompute checks, discrepancy records.',
  regression: 'Official v3 example vectors (valid) and tampered fixtures (invalid) asserting outcomes.',
  fp: 'Round-1 vs round-2 score rounding differences across calculators - use one canonical implementation.',
  related: ['cvss-normalization', 'cvss-v3-analysis', 'cve-severity-analysis'],
  refs: ['CVSS v3.1 equations', 'FIRST examples'],
},
{ file: 'cvss-v4-vector-validation.md', title: 'CVSS v4 Vector Validation', cat: 'validation', keywords: 'v4 vector, validation, recompute',
  purpose: 'Validate CVSS v4.0 vectors: new metric sets, derived metrics, and v4 score recomputation.',
  method: [
    'Parse v4 vectors: base (AV/AC/AT/PR/UI), vulnerable-system impact (VC/VI/VA), subsequent-system impact (SC/SI/SA), plus supplemental (S/A, MSI/MSA, E/AU/R/V/RE/U).',
    'Validate each value against the v4 enumeration; validate the supplemental-system consistency.',
    'Compute derived base metrics (MAV/MAC/MAT/MPR/MUI and impact metrics) where required.',
    'Recompute the v4 score using the official equations; compare with the published value.',
    'Flag discrepancies and version mismatches (v3 vector labeled v4 and vice versa).',
  ],
  evidence: 'v4 validation results with derived-metric computation and recompute comparison.',
  rootCause: 'v4 vectors validated with v3 rules or not validated at all.',
  impact: 'Invalid v4 scores in severity analysis as v4 adoption increases.',
  remediation: 'Version-specific validation, derived-metric computation, recompute checks.',
  regression: 'Official v4 example vectors and invalid fixtures.',
  fp: 'v4 supplemental metric groups absent - they are optional, not errors.',
  related: ['cvss-normalization', 'cvss-v4-analysis', 'cve-severity-analysis'],
  refs: ['CVSS v4.0 specification equations'],
},
{ file: 'cve-vector-conflict-detection.md', title: 'CVE Vector Conflict Detection', cat: 'validation', keywords: 'conflict, vector diff, sources',
  purpose: 'Detect and explain conflicts between CVSS vectors from different sources for the same CVE.',
  method: [
    'Group all vector+version records for the same CVE across sources.',
    'Compare component by component; produce a metric-level diff.',
    'Explain typical causes: different assumptions (network vs local AV), version differences (v3 vs v4), source errors.',
    'Select the canonical vector by source-priority with the diff documented.',
    'Flag unresolved conflicts for review; never silently pick one.',
  ],
  evidence: 'Conflict report: per-CVE vector diffs with component-level explanations and canonical selection.',
  rootCause: 'Conflicting vectors resolved by whichever was read last.',
  impact: 'Severity and environmental assumptions silently wrong.',
  remediation: 'Component-level diffs, cause explanation, documented canonical selection.',
  regression: 'Conflict fixtures asserting diff output and selection.',
  fp: 'Vectors that differ only in normalization (v3.0 vs v3.1 equivalent) - normalize before diffing.',
  related: ['cvss-normalization', 'cve-severity-analysis', 'record-merging'],
  refs: ['CVSS source comparison guidance'],
},
{ file: 'cve-cvss-score-normalization.md', title: 'CVE CVSS Score Normalization', cat: 'validation', keywords: 'score normalization, cross-version',
  purpose: 'Normalize CVSS scores across versions for fair comparison (v2 vs v3 vs v4) with explicit conversion caveats.',
  method: [
    'Tag every score with its CVSS version and source.',
    'For comparison, map scores to a common scale: v3/v4 numeric comparison within version; cross-version comparisons use qualitative bands (None/Low/Medium/High/Critical) with the band table per version.',
    'Never average or convert scores across versions as if equivalent.',
    'Document band-boundary differences between versions (e.g., v3.1 Medium=4.0-6.9 vs v4 Medium=4.0-6.9 but different severity table semantics).',
    'Prefer version-tagged reporting; use bands only for aggregation.',
  ],
  evidence: 'Version-tagged score table with band mapping and a cross-version comparison example.',
  rootCause: 'Mixing v2/v3/v4 scores in one table.',
  impact: 'False ranking equality across versions.',
  remediation: 'Version tags, band-based cross-version comparison, no silent conversion.',
  regression: 'Band-mapping fixtures per version.',
  fp: 'Scores that are qualitively equal across versions but numerically different - use bands, not raw numbers.',
  related: ['cvss-normalization', 'cve-severity-analysis', 'cve-triage-engine'],
  refs: ['CVSS severity band tables v2/v3/v4'],
},
{ file: 'cve-data-consistency-check.md', title: 'CVE Data Consistency Check', cat: 'validation', keywords: 'consistency, cross-field, audit',
  purpose: 'Run cross-field consistency checks on the normalized database: ranges, dates, statuses, and referential integrity.',
  method: [
    'Range consistency: fixed version must not fall inside an affected interval; affected intervals must not be empty or inverted.',
    'Date consistency: published <= lastModified; KEV dateAdded within catalog bounds; dueDate sane.',
    'Status consistency: REJECTED records must not carry exploitation flags; RESERVED records must not be scored.',
    'Referential integrity: related/alias IDs exist; CWE ids resolve in the CWE map; ecosystem/package keys resolve.',
    'Produce a consistency report with violation classes and fix recommendations.',
  ],
  evidence: 'Consistency report with violation counts per class and sample records.',
  rootCause: 'Fields updated independently across sources.',
  impact: 'Contradictory data reaches triage.',
  remediation: 'Scheduled consistency checks, violation classes, fix pipeline.',
  regression: 'Consistency fixtures asserting each violation class is detected.',
  fp: 'Legitimate exceptions (e.g., vendor-issued fixes inside a range for a specific platform) - allow documented exceptions.',
  related: ['cve-data-validation', 'cve-normalization', 'record-merging'],
  refs: ['NVD data integrity notes'],
},
];