// CVE skill data — correlation (12)
export const SKILLS = [
{ file: 'cwe-mapping.md', title: 'CWE Mapping', cat: 'correlation', keywords: 'cwe, weakness map, skill routing',
  purpose: 'Maintain the mapping between CWE IDs and the analysis skills that address them, so CVE triage routes to the right skill automatically.',
  triggers: 'Use whenever a CVE record carries CWE data or when a weakness family needs a skill path.',
  method: [
    'Map each CWE ID (and chain) to the relevant skills: input-validation, injection, auth, session, crypto, storage, business-logic, api, frontend, backend, etc.',
    'Use CWE parent/child hierarchy: a child CWE maps to its parent\'s skills plus child-specific skills.',
    'Give each mapping a confidence (explicit CWE in record = authoritative; inferred = inferred).',
    'Define default routing when no CWE exists: route by vulnerability type from description keywords (tagged inferred).',
    'Keep the mapping in data form so triage can query it.',
  ],
  evidence: 'A queryable CWE→skill mapping table with sample routing decisions (CVE with CWE-89 routes to sql-injection skill).',
  rootCause: 'CWE ignored or treated as a display tag.',
  impact: 'Triage misses the right analysis path.',
  remediation: 'Data-driven mapping, hierarchy-aware queries, confidence tags.',
  regression: 'Routing fixtures asserting the skill path per CWE.',
  fp: 'CWE-79:XSS in records for stored vs reflected variants - routing table differentiates by context.',
  related: ['cwe-family-analysis', 'cve-cwe-correlation', 'cve-triage-engine'],
  refs: ['MITRE CWE', 'CWE hierarchy'],
},
{ file: 'cwe-family-analysis.md', title: 'CWE Family Analysis', cat: 'correlation', keywords: 'cwe family, 29 families, weakness taxonomy',
  purpose: 'Analyze CVEs grouped into the 29 tracked CWE families: recognize the family, know the pattern, and route to the family skill.',
  method: [
    'Track the 29 CWE families with their canonical CWE IDs and primary skills:',
    '1 CWE-20 Improper Input Validation; 2 CWE-22 Path Traversal; 3 CWE-77/78 Command Injection; 4 CWE-79 Cross-Site Scripting; 5 CWE-89 SQL Injection; 6 CWE-91 XML Injection; 7 CWE-93/94 Code Injection; 8 CWE-95 Expression Language Injection; 9 CWE-98 File Inclusion; 10 CWE-113 CRLF/Header Injection; 11 CWE-116 Encoding/Canonicalization; 12 CWE-119/120 Buffer Overflow; 13 CWE-125 Out-of-bounds Read; 14 CWE-287 Authentication; 15 CWE-306 Missing Authentication; 16 CWE-307 Brute-Force; 17 CWE-311/319 cryptography/cleartext; 18 CWE-352 CSRF; 19 CWE-400/770 Resource Exhaustion; 20 CWE-416 Use-After-Free; 21 CWE-434 Unrestricted Upload; 22 CWE-444 HTTP Smuggling; 23 CWE-502 Deserialization; 24 CWE-601 Open Redirect; 25 CWE-611 XXE; 26 CWE-639/284 IDOR/BOLA; 27 CWE-798 Hardcoded Credentials; 28 CWE-862/863 Broken Access Control; 29 CWE-918 SSRF.',
    'For each CVE, classify into its family with the CWE + description evidence.',
    'Summarize per-family statistics across the project for systemic-risk reporting.',
    'Keep the family table stable and documented (extension permitted with rationale).',
  ],
  evidence: 'Family-classified CVE set with a per-family summary table.',
  rootCause: 'Weakness type treated as a single tag instead of a family.',
  impact: 'Cross-CVE pattern blindness (e.g., many input-validation CVEs in one dependency cluster).',
  remediation: 'Canonical family taxonomy, classification with evidence, per-family reports.',
  regression: 'Family-classification fixtures per family id.',
  fp: 'Chained CWEs classified under the wrong parent (e.g., CWE-79:CWE-80) - preserve the chain order.',
  related: ['cwe-mapping', 'cve-cwe-correlation', 'cve-triage-engine'],
  refs: ['CWE Top 25', 'MITRE CWE list'],
},
{ file: 'cve-cwe-correlation.md', title: 'CVE CWE Correlation', cat: 'correlation', keywords: 'cve to cwe, weakness link, chains',
  purpose: 'Correlate CVE records to their CWE(s) with confidence, supporting weakness-based queries and trend analysis.',
  method: [
    'Gather CWE references from NVD (weaknesses array), advisories, and patches; normalize via cwe-normalization.',
    'Prioritize authoritative CWE refs over inferred ones; record confidence.',
    'Handle chains (CWE-79:CWE-80) and parent/child CWE relationships in queries.',
    'Support queries: all CVEs of a CWE, CWE trends over time, dependency-CWE clusters.',
    'Flag records with no CWE as UNKNOWN weakness (route by description keywords, tagged inferred).',
  ],
  evidence: 'Correlation index: CVE→CWE edges with confidence; sample queries executed.',
  rootCause: 'CWE data scattered and unjoined.',
  impact: 'No weakness-level visibility across the CVE database.',
  remediation: 'Indexed CVE-CWE edges, confidence-aware queries, UNKNOWN tagging.',
  regression: 'Correlation fixtures asserting edges and chain handling.',
  fp: 'Parent-CWE queries returning child matches as exact matches - label hierarchy expansions.',
  related: ['cwe-normalization', 'cwe-mapping', 'cwe-family-analysis'],
  refs: ['NVD weaknesses field'],
},
{ file: 'cve-package-correlation.md', title: 'CVE Package Correlation', cat: 'correlation', keywords: 'cve to package, affected package, version map',
  purpose: 'Correlate CVEs to packages (ecosystem, package name, affected/fixed versions) to power dependency-match queries.',
  method: [
    'Build the CVE→package index from OSV affected[], GHSA, NVD CPE product mapping (with confidence).',
    'Normalize package names per ecosystem (package-identity-resolution).',
    'Store affected ranges and fixed versions per (cve, package) with source provenance.',
    'Support the query path: package+version → candidate CVEs (discovery), CVE → affected packages (correlation).',
    'Handle multi-package CVEs (a CVE affecting several packages/ecosystems) without duplicate fanout errors.',
  ],
  evidence: 'A CVE→package index with a sample query returning the correct candidate set with provenance.',
  rootCause: 'CVE-package correlation done ad-hoc per scan.',
  impact: 'Repeated, inconsistent matching; missing package coverage.',
  remediation: 'Indexed correlation, normalized names, provenance-backed ranges.',
  regression: 'Correlation fixtures: one CVE many packages, one package many CVEs.',
  fp: 'CPE-product matches that are platform-level, not package-level (e.g., "linux kernel") - tag granularity.',
  related: ['cve-version-matching', 'package-identity-resolution', 'cve-dependency-discovery'],
  refs: ['OSV affected data', 'NVD configurations'],
},
{ file: 'cve-fixed-version.md', title: 'CVE Fixed Version', cat: 'correlation', keywords: 'fixed version index, patched',
  purpose: 'Correlate CVEs with their fixed/patch versions across sources to support fix queries and upgrade planning.',
  method: [
    'Collect fixed versions from OSV/GHSA events, vendor advisories, distro trackers, and patch metadata.',
    'Normalize per ecosystem and index by (package, ecosystem, version).',
    'Support queries: given package+version, what CVEs does it fix; given CVE, what is the first fixed version per source.',
    'Preserve source conflicts with provenance (one CVE with multiple first-fixed by distro).',
    'Feed the remediation engine with the resolved target.',
  ],
  evidence: 'Fixed-version index with a sample query and conflict documentation.',
  rootCause: 'Fix data buried in advisories, unavailable to automated checks.',
  impact: 'Upgrade and verification cannot confirm fixes automatically.',
  remediation: 'Indexed fix data, per-source values, conflict preservation.',
  regression: 'Fix-index fixtures asserting queries and conflict handling.',
  fp: 'Fixed versions that only apply to specific distro backports - document applicability.',
  related: ['fixed-version-extraction', 'cve-fix-version-resolution', 'cve-version-matching'],
  refs: ['OSV fixed events'],
},
{ file: 'cve-advisory-correlation.md', title: 'CVE Advisory Correlation', cat: 'correlation', keywords: 'cve advisory link, evidence set',
  purpose: 'Correlate the full advisory evidence set for each CVE (vendor, GitHub, OSV, distro, ecosystem) into a single reviewable record.',
  method: [
    'For each canonical CVE, gather all linked advisories from the ingestion layer (advisory-correlation).',
    'Summarize consensus and disagreement: affected ranges, fixed versions, severity.',
    'Produce per-CVE evidence bundles for triage and reporting (links, quoted claims, dates).',
    'Track advisory publication/changes over time for re-triage triggers.',
    'Store the bundle in the correlation index.',
  ],
  evidence: 'Per-CVE evidence bundles with consensus/dissensus summaries and change tracking.',
  rootCause: 'Triage reads one advisory and misses contradicting data.',
  impact: 'Verdicts based on incomplete evidence.',
  remediation: 'Evidence-bundle correlation, consensus summaries, change detection.',
  regression: 'Bundle fixtures asserting summary correctness.',
  fp: 'Advisories for different products with the same CVE - keep product context per advisory.',
  related: ['advisory-correlation', 'record-merging', 'cve-triage-engine'],
  refs: ['advisory source list'],
},
{ file: 'vendor-correlation.md', title: 'Vendor Correlation', cat: 'correlation', keywords: 'vendor map, product lines, cna',
  purpose: 'Correlate vendors/products across CVEs and advisories: vendor keys, product lines, CNAs, and per-vendor advisory feeds.',
  method: [
    'Build the vendor map: canonical vendor keys, aliases, product lines (from CPE, advisory heads, OSV vendorProject).',
    'Map CVEs to vendors/products; cluster by product line for portfolio-level reporting.',
    'Track per-vendor advisory sources and their update cadence (vendors map to source-priority rules).',
    'Support queries: all CVEs for a vendor/product line, vendor notification status (affects disclosure workflows).',
    'Handle renames/forks of products with provenance (see package-identity-resolution).',
  ],
  evidence: 'Vendor map with product-line clusters and a sample portfolio query.',
  rootCause: 'Vendor names treated as display strings.',
  impact: 'No portfolio-level CVE visibility; missed vendor-consolidated data.',
  remediation: 'Canonical vendor keys, product-line clusters, per-vendor source config.',
  regression: 'Vendor alias fixtures asserting canonicalization.',
  fp: 'Two vendors sharing a CPE product name (common for standards-based products) - disambiguate by vendor key.',
  related: ['cpe-normalization', 'vendor-advisory-ingestion', 'source-priority'],
  refs: ['CPE vendor field', 'CNA assignments'],
},
{ file: 'cve-search-engine.md', title: 'CVE Search Engine', cat: 'correlation', keywords: 'search, query, index',
  purpose: 'Operate the conceptual CVE search/index layer: lookup by CVE ID, package, version, CWE, vendor, keyword — always offline-first with EXPLICIT-UNKNOWN on missing data.',
  method: [
    'Maintain the searchable index over normalized records: IDs, package names, versions, CWEs, vendors, descriptions.',
    'Support precise lookups: CVE ID via canonical ID index; package+version match; range membership.',
    'Support fuzzy/keyword search over titles/descriptions with relevance ranking (no fabricated results).',
    'For any query dimension with no data, return EXPLICIT-UNKNOWN (never empty-is-safe).',
    'Design for offline use: the corpus is the local database; network is an update path, not a query path.',
  ],
  evidence: 'Query examples run against the index with correct result sets and UNKNOWN handling.',
  rootCause: 'Search delegated to an external API with no local index.',
  impact: 'Unavailable data at review time; fabricated impressions of coverage.',
  remediation: 'Local index + offline query path, explicit unknown results, incremental update.',
  regression: 'Query fixtures asserting result sets and UNKNOWN returns.',
  fp: 'Fuzzy matches presented as CVE matches - separate "found similar" from "found exact".',
  related: ['cve-normalization', 'cve-data-validation', 'cve-triage-engine'],
  refs: ['search index design'],
},
{ file: 'cve-root-cause-analysis.md', title: 'CVE Root Cause Analysis', cat: 'correlation', keywords: 'root cause, pattern, flaw type',
  purpose: 'Derive and correlate root-cause patterns across CVEs (from patches and descriptions) to spot systemic classes affecting the project.',
  method: [
    'For reviewed CVEs, extract root-cause patterns: missing validation, unsafe parse, logic error, unchecked index, protocol ambiguity.',
    'Correlate patterns across the project inventory: which dependencies share the pattern class.',
    'Map patterns to remediation families (validate input, upgrade parser, enable-safe-mode).',
    'Report systemic root-cause clusters as the strategic finding (higher value than per-CVE fixes).',
    'Base every pattern claim on patch/description evidence.',
  ],
  evidence: 'Root-cause pattern table per CVE with evidence and the cluster analysis.',
  rootCause: 'Per-CVE fixes without understanding the underlying pattern.',
  impact: 'Repeated fixes for the same root cause across components.',
  remediation: 'Pattern correlation, remediation families, systemic reporting.',
  regression: 'Pattern-extraction fixtures with known patches.',
  fp: 'Similar symptoms from different root causes - classify by the actual code defect.',
  related: ['cve-patch-analysis', 'cwe-family-analysis', 'cve-triage-engine'],
  refs: ['root cause taxonomy'],
},
{ file: 'cve-ecosystem-correlation.md', title: 'CVE Ecosystem Correlation', cat: 'correlation', keywords: 'ecosystem view, cross-package, supply chain',
  purpose: 'Correlate CVEs at the ecosystem level: cross-package clusters, ecosystem-wide attack patterns, and supply-chain exposure.',
  method: [
    'Cluster CVEs by ecosystem (npm, PyPI, Maven, ...) and by shared dependency lineage.',
    'Identify ecosystem-wide patterns: a parser library vulnerability propagating to all consumers.',
    'Assess supply-chain exposure: compromised packages (or histories) interacting with the project inventory.',
    'Produce ecosystem-risk summaries for reporting.',
    'Keep ecosystem cluster data queryable alongside per-CVE records.',
  ],
  evidence: 'Ecosystem cluster report with cross-package ties and supply-chain notes.',
  rootCause: 'Per-package analysis hides ecosystem-level propagation.',
  impact: 'Missed propagation when one library is fixed but forks/relatives remain vulnerable.',
  remediation: 'Ecosystem clustering, lineage tracking, propagation checks.',
  regression: 'Cluster fixtures asserting propagation detection.',
  fp: 'Shared-name packages in different ecosystems correlated without identity resolution.',
  related: ['ecosystem-registry-map', 'cve-package-correlation', 'cve-transitive-dependency-analysis'],
  refs: ['supply-chain risk frameworks'],
},
{ file: 'cve-exploit-correlation.md', title: 'CVE Exploit Correlation', cat: 'correlation', keywords: 'exploit mapping, poc, exploit-db',
  purpose: 'Correlate CVEs with public exploit/PoC availability and provenance for risk signals (not exploit creation).',
  method: [
    'For each CVE, search authoritative exploit indexes (Exploit-DB, Metasploit, GitHub) for PoC/exploit records.',
    'Record per result: source, URL, date, type (PoC/verified exploit), confidence in the CVE mapping.',
    'Differentiate: PoC exists (weaponization interest) vs confirmed in-the-wild (KEV) - labels must not blur.',
    'Include this signal in triage inputs only (priority boost), never as applicability proof.',
    'Keep results citable in reports with the source.',
  ],
  evidence: 'Exploit-correlation table with per-item provenance and source/date.',
  rootCause: 'Exploit availability inferred from memory or single sources.',
  impact: 'Mis-prioritized reviews (both directions).',
  remediation: 'Indexed correlation, dual-label reporting, provenance.',
  regression: 'Correlation fixtures asserting PoC vs in-the-wild labeling.',
  fp: 'Non-functional PoCs counted as exploit-ready - mark availability without verification as weaker signal.',
  related: ['cve-exploitation-status-analysis', 'cve-known-exploitation-priority', 'cve-triage-engine'],
  refs: ['Exploit-DB', 'Metasploit', 'GitHub codesearch'],
},
{ file: 'cve-timeline-analysis.md', title: 'CVE Timeline Analysis', cat: 'correlation', keywords: 'timeline, disclosure, patch lag',
  purpose: 'Analyze CVE timelines: publication, patch availability, NVD enrichment, KEV addition — to reason about exposure windows and data freshness.',
  method: [
    'Record per CVE: datePublished, dateUpdated, patch availability date (best evidence), KEV dateAdded (if any).',
    'Compute exposure windows: time from publication to local awareness (ingestion date), to patch, to remediation.',
    'Detect stale data: records not updated in NVD/advisory since X, KEV due-dates passed.',
    'Flag aged-but-unpatched CVEs in the triage queue.',
    'Report timeline summaries for management.',
  ],
  evidence: 'Timeline table with windows computed and stale-data flags.',
  rootCause: 'Timestamps ignored; freshness never assessed.',
  impact: 'Silently outdated data used for decisions.',
  remediation: 'Timeline tracking, freshness flags, re-triage on updates.',
  regression: 'Timeline fixtures asserting window computation.',
  fp: 'Missing patch dates treated as "no patch" - UNKNOWN, not absence.',
  related: ['cve-triage-engine', 'cve-data-consistency-check', 'cisa-kev-ingestion'],
  refs: ['NVD timestamps', 'KEV dates'],
},
];