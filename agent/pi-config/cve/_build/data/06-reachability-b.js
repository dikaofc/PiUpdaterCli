// CVE skill data — reachability B (4)
export const SKILLS = [
{ file: 'execution-path-analysis.md', title: 'Execution Path Analysis', cat: 'reachability', keywords: 'execution path, runtime entry, deployment',
  purpose: 'Verify the call path can execute in the deployed configuration: entry points enabled, jobs scheduled, routes mounted.',
  method: [
    'Map runtime entry points in the deployment: HTTP routes mounted, queue consumers running, cron/schedulers active, event listeners subscribed.',
    'Check that the entry point leading to the vulnerable path is enabled in the deployed configuration.',
    'Account for runtime context: conditional middleware, environment-specific route mounting, disabled workers.',
    'Verdict: path executes in deployment / only in specific configurations / does not execute.',
    'Record configuration evidence for the verdict.',
  ],
  evidence: 'Entry-point enablement evidence (routing config, worker list, deployment manifests) tied to the call path.',
  rootCause: 'Static call graph ignoring deployment-time enablement.',
  impact: 'Reachable-in-code but never-executed vulnerabilities reported as exploitable.',
  remediation: 'Deployment-aware entry analysis, config evidence, dynamic checks where feasible.',
  regression: 'Fixture deployments asserting path enablement verdicts.',
  fp: 'Feature-flagged entries disabled in production but enabled in CI/test config - use production config.',
  related: ['cve-reachability-engine', 'call-graph-analysis', 'cve-configuration-applicability'],
  refs: ['deployment manifests, route configs'],
},
{ file: 'sensitive-operation-analysis.md', title: 'Sensitive Operation Analysis', cat: 'reachability', keywords: 'sensitive operation, impact sink, privilege',
  purpose: 'Determine the sensitive operation reachable via the vulnerable path (code execution, data read/write, auth bypass, DoS) to ground impact.',
  method: [
    'Classify the vulnerable function capability: memory corruption (RCE potential), injection sinks, auth decisions, data access, resource consumption.',
    'Assess what the path can actually reach: does the attacker-controlled input reach the sensitive operation?',
    'Determine privilege context: unauthenticated vs authenticated vs admin-only reachability.',
    'Estimate exploitation requirements from the CVE (complexity, interaction) combined with the local path.',
    'Output the impact claim tied to the sensitive operation, not the CVE generic description.',
  ],
  evidence: 'Sensitive-operation chain: path, capability, privilege context, with the impact claim bounded by it.',
  rootCause: 'Generic CVE impact copied without local path grounding.',
  impact: 'Impact overstated (copy-paste CVSS narrative) or understated.',
  remediation: 'Path-grounded impact statements, privilege-context labels, exploitability requirements.',
  regression: 'Impact-chain fixtures asserting correct capability classification.',
  fp: 'Sensitive operation present but not reachable via the vulnerable path - keep classification per path.',
  related: ['cve-reachability-engine', 'input-source-analysis'],
  refs: ['CWE capability taxonomy'],
},
{ file: 'reachability-classification.md', title: 'Reachability Classification', cat: 'reachability', keywords: 'classification, directly reachable, unknown',
  purpose: 'Assign and record the standardized reachability classification for each CVE candidate.',
  method: [
    'Apply the taxonomy exactly: DIRECTLY_REACHABLE - the vulnerable function is called on an attacker-influenceable path in the deployed configuration (evidence required).',
    'TRANSITIVELY_REACHABLE - reachable through an intermediate dependency call chain (the path runs through another package).',
    'PRESENT_BUT_UNUSED - the vulnerable code is present but no project code invokes it.',
    'CONDITIONALLY_REACHABLE - reachable only under specific configuration/feature/privilege conditions (name the condition).',
    'UNREACHABLE - proven that no path exists (e.g., function absent, feature disabled, no imports).',
    'UNKNOWN - insufficient evidence; do not default to reachable.',
    'Record the classification with the evidence level and any condition, on the CVE record.',
  ],
  evidence: 'Classification per CVE with the supporting evidence link and condition statement; a classification summary table.',
  rootCause: 'Missing standardized classification; "in lockfile" defaulted to "reachable".',
  impact: 'Unreliable priority lists and false exploitability claims.',
  remediation: 'Standardized taxonomy, evidence-linked classifications, UNKNOWN-first default.',
  regression: 'Classification mapping tests; report gates requiring classification before severity finalization.',
  fp: 'Classify the path, not the package: the same CVE can be DIRECTLY_REACHABLE in one service and PRESENT_BUT_UNUSED in another.',
  related: ['cve-reachability-engine', 'cve-triage-engine', 'cve-severity-analysis'],
  refs: ['GitHub reachability studies', 'snyk reachability'],
},
{ file: 'reachability-evidence-model.md', title: 'Reachability Evidence Model', cat: 'reachability', keywords: 'evidence level, e0-e5, proof chain',
  purpose: 'Assign evidence levels to reachability verdicts so that claims are graded by the strength of the proof behind them.',
  method: [
    'Use the evidence scale: E0 none, E1 lockfile/manifest only, E2 package presence + version match, E3 import/call evidence, E4 call path + input source traced, E5 verified execution in deployed configuration.',
    'Map each reachability classification to the minimum evidence level that supports it (DIRECTLY_REACHABLE requires E4+; PRESENT_BUT_UNUSED requires E3 import absence check).',
    'Attach the evidence level to the recorded classification and to any report claim.',
    'Downgrade claims automatically when evidence is missing (a claim without its required level reverts to UNKNOWN).',
    'Keep the evidence record per CVE: what was inspected, when, and by which tool/person.',
  ],
  evidence: 'Per-CVE evidence levels with classification mapping; a report sample showing E-level attribution.',
  rootCause: 'Verdicts asserted without graded proof.',
  impact: 'Confidence inflation; unverifiable claims in reports.',
  remediation: 'Evidence-level gate for each classification, automatic downgrade on missing evidence.',
  regression: 'Evidence-gate tests asserting downgrade behavior.',
  fp: 'Tool output cited as evidence without confirming the tool inspected the right artifact.',
  related: ['reachability-classification', 'cve-reachability-engine', 'cve-confidence-model'],
  refs: ['evidence model in root docs'],
},
];