# databases/vendor

Vendor advisories cache. One directory per vendor (apache, microsoft, github, ...) with advisories fetched by release/security feeds or manual imports.

> Placeholder directory. The CVE engine treats this location as runtime data —
> contents are populated by ingestion/indexing pipelines and may be large.
