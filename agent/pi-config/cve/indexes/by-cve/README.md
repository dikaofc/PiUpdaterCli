# indexes/by-cve

Index: CVE ID → record file. Layout: cve/skills-less records normalized under vulnerability-data/normalized/; this dir stores lookup shards (e.g., by first digits of the sequential part) mapping to normalized record paths.

> Placeholder directory. The CVE engine treats this location as runtime data —
> contents are populated by ingestion/indexing pipelines and may be large.
